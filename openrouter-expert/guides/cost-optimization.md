# 💰 OpenRouter 비용 제로 최적화 가이드

> 무료로만 AI 서비스를 이용하기 위한 완벽 전략

---

## 🎯 핵심 원칙

```
┌──────────────────────────────────────────────────────┐
│                  비용 제로 3대 원칙                    │
├──────────────────────────────────────────────────────┤
│  1. Credit Limit $0 설정       → 과금 원천 차단       │
│  2. 무료 모델(:free)만 사용     → 비용 발생 없음       │
│  3. Rate Limit 내 사용         → 서비스 중단 없음     │
└──────────────────────────────────────────────────────┘
```

---

## 1️⃣ 과금 원천 차단 설정

### Credit Limit 설정
```
경로: openrouter.ai → Keys → [키 선택] → Edit

설정값: Credit Limit = $0

효과:
├── 무료 모델 → 정상 작동
├── 유료 모델 → 요청 차단 (에러)
└── 실수 → 비용 발생 없음
```

### API 키 보안 설정
```python
# ❌ 절대 금지
api_key = "sk-or-v1-실제키값"

# ✅ 올바른 방법
import os
api_key = os.getenv("OPENROUTER_API_KEY")
```

---

## 2️⃣ 무료 모델 완벽 활용

### 현재 사용 가능한 무료 모델
```
모델 ID                                    │ 특징
──────────────────────────────────────────┼────────────
meta-llama/llama-3.1-8b-instruct:free     │ 최고 추천
google/gemma-2-9b-it:free                 │ 경량/빠름
```

### 모델 선택 가이드
| 작업 유형 | 추천 모델 | 이유 |
|----------|----------|------|
| 일반 대화 | Llama 3.1 8B | 자연스러운 대화 |
| 질문 답변 | Llama 3.1 8B | 정확한 정보 |
| 간단한 요약 | Gemma 2 9B | 빠른 처리 |
| 코드 도움 | Llama 3.1 8B | 코딩 능력 우수 |
| 번역 | Llama 3.1 8B | 다국어 지원 |

### 무료 모델 확인 방법
```
1. openrouter.ai/models 접속
2. "Free" 필터 선택
3. 모델명에 :free 확인
```

---

## 3️⃣ Rate Limit 효율적 관리

### 무료 플랜 한도
```
┌────────────────────────────────┐
│  일일 한도: 50회              │
│  분당 한도: 20회              │
│  제공업체: 4개                │
│  무료 모델: 25개+             │
└────────────────────────────────┘
```

### 일일 50회 최적 배분
```
시간대          │ 배분  │ 사용 목적
───────────────┼──────┼───────────────
오전 (9-12시)  │ 15회 │ 중요 업무
점심 (12-14시) │  5회 │ 가벼운 질문
오후 (14-18시) │ 20회 │ 핵심 작업
저녁 (18-22시) │ 10회 │ 학습/실험
───────────────┴──────┴───────────────
               총 50회
```

### Rate Limit 에러 대응 코드
```python
import time

def smart_request(client, messages, max_retries=3):
    """Rate Limit을 고려한 스마트 요청"""
    for attempt in range(max_retries):
        try:
            response = client.chat.completions.create(
                model="meta-llama/llama-3.1-8b-instruct:free",
                messages=messages
            )
            return response.choices[0].message.content
        except Exception as e:
            error_msg = str(e)
            if "429" in error_msg:
                wait = 2 ** attempt  # 지수 백오프
                print(f"⏳ Rate Limit! {wait}초 대기...")
                time.sleep(wait)
            else:
                raise e
    return None
```

---

## 4️⃣ 요청 최적화 전략

### 프롬프트 최적화
```
❌ 비효율적 (여러 번 요청 필요)
───────────────────────────────
요청1: "파이썬이 뭐야?"
요청2: "파이썬 예제 보여줘"
요청3: "파이썬 장점이 뭐야?"

✅ 효율적 (한 번에 해결)
───────────────────────────────
요청: "파이썬이 무엇인지, 간단한 예제 코드,
      그리고 주요 장점 3가지를 알려주세요."
```

### 시스템 프롬프트 활용
```python
# 시스템 프롬프트로 답변 품질 향상
messages = [
    {
        "role": "system",
        "content": """당신은 전문 프로그래밍 튜터입니다.
        - 한국어로 답변
        - 예제 코드 포함
        - 핵심만 간결하게"""
    },
    {"role": "user", "content": "리스트 컴프리헨션 설명해줘"}
]
```

### 결과 캐싱 전략
```python
import json
import os

CACHE_FILE = "ai_cache.json"

def load_cache():
    if os.path.exists(CACHE_FILE):
        with open(CACHE_FILE, 'r', encoding='utf-8') as f:
            return json.load(f)
    return {}

def save_cache(cache):
    with open(CACHE_FILE, 'w', encoding='utf-8') as f:
        json.dump(cache, f, ensure_ascii=False, indent=2)

def cached_request(client, prompt):
    """동일 질문은 캐시에서 반환"""
    cache = load_cache()

    if prompt in cache:
        print("📦 캐시에서 반환")
        return cache[prompt]

    response = client.chat.completions.create(
        model="meta-llama/llama-3.1-8b-instruct:free",
        messages=[{"role": "user", "content": prompt}]
    )
    result = response.choices[0].message.content

    cache[prompt] = result
    save_cache(cache)

    return result
```

---

## 5️⃣ 사용량 모니터링

### 대시보드 확인
```
경로: openrouter.ai → Activity

확인 항목:
├── 일일 요청 수
├── 사용한 모델
├── 토큰 사용량
└── 잔여 한도
```

### 코드에서 사용량 추적
```python
class UsageTracker:
    def __init__(self, daily_limit=50):
        self.daily_limit = daily_limit
        self.used_today = 0

    def can_request(self):
        return self.used_today < self.daily_limit

    def request(self, client, messages):
        if not self.can_request():
            print(f"⚠️ 일일 한도 도달! ({self.used_today}/{self.daily_limit})")
            return None

        response = client.chat.completions.create(
            model="meta-llama/llama-3.1-8b-instruct:free",
            messages=messages
        )
        self.used_today += 1
        print(f"📊 사용량: {self.used_today}/{self.daily_limit}")

        return response.choices[0].message.content

# 사용 예시
tracker = UsageTracker()
result = tracker.request(client, [{"role": "user", "content": "안녕!"}])
```

---

## 📊 비용 비교

### OpenRouter 무료 vs 유료 서비스
| 항목 | OpenRouter 무료 | ChatGPT Plus | Claude Pro |
|------|----------------|--------------|------------|
| 월 비용 | **$0** | $20 | $20 |
| 일일 한도 | 50회 | ~100회 | ~100회 |
| API 접근 | ✅ | ❌ | ❌ |
| 코드 통합 | ✅ | ❌ | ❌ |

---

## ✅ 체크리스트

```
비용 제로 운영 체크리스트:

[ ] Credit Limit $0 설정 완료
[ ] 환경 변수로 API 키 관리
[ ] 무료 모델(:free)만 사용
[ ] 일일 50회 한도 준수
[ ] 프롬프트 최적화 적용
[ ] 결과 캐싱 구현 (선택)
[ ] 사용량 모니터링 설정
```

---

## 🚨 경고 상황 대응

### 상황별 대응
| 상황 | 증상 | 대응 |
|------|------|------|
| 한도 초과 | 429 에러 | 다음 날까지 대기 |
| 키 노출 의심 | 비정상 사용량 | 즉시 키 삭제/재생성 |
| 유료 모델 실수 | Credit 부족 에러 | Credit Limit $0 확인 |

---

> 💡 **결론**: 올바른 설정 + 효율적 사용 = 완전 무료로 고품질 AI 서비스 이용 가능!
